# Lumen — Seeded Vulnerability Answer Key
# DO NOT SHOW IN DEMO. This is the ground truth for eval.

All `severity` and `category` values are exact strings from
`shared/schema.py` (Severity / Category enums). All `vulnerable_code`
blocks are verbatim slices of the source, including leading whitespace,
and appear in the named file at `line_start..line_end` inclusive.

---

## Vuln 04 — Command injection via subprocess shell=True with user hostname
- file: demo-repo/admin/diagnostics.py
- line_start: 28
- line_end: 28
- severity: critical
- category: command_injection
- cwe: CWE-78
- description: The hostname from the request body is concatenated into a shell command run with shell=True, letting an admin-route caller chain arbitrary commands with `;`, `&&`, or backticks.
- vulnerable_code: |

```
    output = subprocess.check_output(cmd, shell=True, text=True, timeout=10)
```

---

## Decoys (must NOT be flagged)
- file: demo-repo/safe/queries.py — parameterized query, looks like concatenation. Uses `cursor.execute(sql, (email, status))` with `?` placeholders throughout `CustomerQueries`. No string interpolation of user input into SQL anywhere in the file.
- file: demo-repo/safe/shell_utils.py — allowlisted subprocess, looks like command injection. `run_diagnostic` rejects any `cmd` not in `ALLOWED_COMMANDS = {"status", "version", "health"}` before invoking `subprocess.run(["./bin/diag", cmd], shell=False, ...)` with an arg list.

## Vendor (must be SKIPPED, not just unflagged)
- file: demo-repo/vendor/site-packages/flask_helper/__init__.py — contains a vuln-shaped pattern (`f"SELECT ... WHERE name = '{name}'"`) in vendored code. The scanner must skip `/vendor/` entirely as part of its first-party-only rule. This file exists specifically to demonstrate that skip on stage.
