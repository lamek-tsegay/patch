# Lumen — Seeded Vulnerability Answer Key
# DO NOT SHOW IN DEMO. This is the ground truth for eval.

All `severity` and `category` values are exact strings from
`shared/schema.py` (Severity / Category enums). All `vulnerable_code`
blocks are verbatim slices of the source, including leading whitespace,
and appear in the named file at `line_start..line_end` inclusive.

---

## Vuln 01 — SQL injection in login auth path
- file: demo-repo/auth/login.py
- line_start: 42
- line_end: 44
- severity: critical
- category: sql_injection
- cwe: CWE-89
- description: User-supplied email is interpolated directly into a SQL string with no parameterization; an attacker can bypass authentication or exfiltrate the users table.
- vulnerable_code: |

```
    query = f"SELECT id, password_hash FROM users WHERE email = '{email}'"
    row = db.execute(query).fetchone()
    if not row:
```

---

## Vuln 02 — Hardcoded production AWS secret access key
- file: demo-repo/config.py
- line_start: 30
- line_end: 30
- severity: critical
- category: hardcoded_secret
- cwe: CWE-798
- description: A long-lived AWS secret access key is hardcoded into application config instead of read from the environment or a secrets manager.
- vulnerable_code: |

```
    aws_secret_access_key = "Hk9d/2vL+sN4qXc7TbR8aZmKp1wJ5fGyEoD3iU+a"
```

---

## Vuln 03 — Insecure deserialization via pickle.loads on request body
- file: demo-repo/admin/import_data.py
- line_start: 42
- line_end: 42
- severity: critical
- category: insecure_deserialization
- cwe: CWE-502
- description: The admin import endpoint passes the raw request body to pickle.loads, which allows arbitrary code execution by any caller able to reach the route.
- vulnerable_code: |

```
    records = pickle.loads(blob)
```

---

## Vuln 04 — Command injection via subprocess shell=True with user hostname
- file: demo-repo/admin/diagnostics.py
- line_start: 28
- line_end: 34
- severity: critical
- category: command_injection
- cwe: CWE-78
- description: The hostname from the request body is interpolated into a shell command run with shell=True, letting an admin-route caller chain arbitrary commands with `;`, `&&`, or backticks.
- vulnerable_code: |

```
    result = subprocess.run(
        f"ping -c 2 {hostname}",
        shell=True,
        capture_output=True,
        text=True,
        timeout=10,
    )
```

---

## Vuln 05 — Stored XSS via |safe filter on user-submitted comment body
- file: demo-repo/templates/comment.html
- line_start: 9
- line_end: 9
- severity: high
- category: xss_stored
- cwe: CWE-79
- description: Comment bodies submitted by users are rendered with the |safe filter, disabling Jinja autoescape and allowing persisted HTML/JS to execute in every viewer's browser.
- vulnerable_code: |

```
    {{ comment.body|safe }}
```

---

## Vuln 06 — SSRF via requests.get of user-supplied URL with no allowlist
- file: demo-repo/integrations/url_import.py
- line_start: 29
- line_end: 29
- severity: high
- category: ssrf
- cwe: CWE-918
- description: Any authenticated (non-admin) user can cause the server to fetch an arbitrary URL, reaching internal services on localhost / 169.254.169.254 / RFC1918 ranges.
- vulnerable_code: |

```
    response = requests.get(user_url, timeout=10)
```

---

## Vuln 07 — Path traversal via os.path.join on user-supplied filename
- file: demo-repo/files/download.py
- line_start: 30
- line_end: 30
- severity: high
- category: path_traversal
- cwe: CWE-22
- description: The `name` query parameter is joined to UPLOAD_DIR with no canonicalization or containment check; `?name=../../../etc/passwd` escapes the upload root.
- vulnerable_code: |

```
    path = os.path.join(UPLOAD_DIR, file_id, filename)
```

---

## Vuln 08 — Broken access control: require_admin accepts any logged-in user
- file: demo-repo/auth/routes.py
- line_start: 49
- line_end: 49
- severity: high
- category: broken_access_control
- cwe: CWE-285
- description: The require_admin decorator checks only that the user is logged in (`if user:`) instead of verifying `user.is_admin`, so any authenticated account can hit admin-only endpoints (`/admin/import`, `/admin/diagnostics/ping`).
- vulnerable_code: |

```
        if user:
```

---

## Vuln 09 — Weak crypto: MD5 used for password hashing
- file: demo-repo/auth/models.py
- line_start: 18
- line_end: 18
- severity: medium
- category: weak_crypto
- cwe: CWE-327
- description: Passwords are hashed with unsalted MD5, which is fast and broken; recovered hashes from any DB dump can be reversed via rainbow tables in seconds.
- vulnerable_code: |

```
    return hashlib.md5(password.encode("utf-8")).hexdigest()
```

---

## Vuln 10 — Reflected XSS via |safe on query param echoed into error page
- file: demo-repo/templates/error.html
- line_start: 5
- line_end: 5
- severity: medium
- category: xss_reflected
- cwe: CWE-79
- description: The `q` query parameter is rendered with |safe on the error page, so a crafted link executes attacker-controlled HTML/JS in the victim's browser.
- vulnerable_code: |

```
  <p>We couldn't process your request for: {{ request.args.get('q')|safe }}</p>
```

---

## Vuln 11 — Insecure random for password reset tokens
- file: demo-repo/admin/tokens.py
- line_start: 18
- line_end: 18
- severity: medium
- category: insecure_random
- cwe: CWE-338
- description: Password reset tokens are built from random.randint, which is a Mersenne Twister PRNG; given a handful of observed tokens its internal state can be recovered and future tokens predicted.
- vulnerable_code: |

```
    token = "".join(ALPHABET[random.randint(0, len(ALPHABET) - 1)] for _ in range(32))
```

---

## Vuln 12 — Verbose error handler leaks stack traces to response body
- file: demo-repo/errors.py
- line_start: 15
- line_end: 22
- severity: low
- category: information_disclosure
- cwe: CWE-209
- description: The global exception handler returns the full Python traceback in the JSON response body, exposing internal file paths, library versions, and code structure to any caller who triggers an error.
- vulnerable_code: |

```
        tb = traceback.format_exc()
        log.exception("uncaught exception")
        return jsonify({
            "error": "internal server error",
            "type": exc.__class__.__name__,
            "message": str(exc),
            "traceback": tb,
        }), 500
```

---

## Vuln 13 — Hardcoded Stripe live API key in source
- file: demo-repo/payments/stripe_webhook.py
- line_start: 16
- line_end: 16
- severity: high
- category: hardcoded_secret
- cwe: CWE-798
- description: A live Stripe secret API key (sk_live_...) is committed as a module-level constant; anyone with read access to the repo can issue charges and refunds against the connected account.
- vulnerable_code: |

```
STRIPE_API_KEY = "sk_live_51Hq8aL2eZvKYlo2C8nLbXxYzAbCdEfGhIjKlMnOpQrStUvWxYz0123456789abcdefABCDEFghijklMNOPQRSTuvwxYZ"
```

---

## Decoys (must NOT be flagged)
- file: demo-repo/safe/queries.py — parameterized query, looks like concatenation. Uses `cursor.execute(sql, (email, status))` with `?` placeholders throughout `CustomerQueries`. No string interpolation of user input into SQL anywhere in the file.
- file: demo-repo/safe/shell_utils.py — allowlisted subprocess, looks like command injection. `run_diagnostic` rejects any `cmd` not in `ALLOWED_COMMANDS = {"status", "version", "health"}` before invoking `subprocess.run(["./bin/diag", cmd], shell=False, ...)` with an arg list.

## Vendor (must be SKIPPED, not just unflagged)
- file: demo-repo/vendor/site-packages/flask_helper/__init__.py — contains a vuln-shaped pattern (`f"SELECT ... WHERE name = '{name}'"`) in vendored code. The scanner must skip `/vendor/` entirely as part of its first-party-only rule. This file exists specifically to demonstrate that skip on stage.
