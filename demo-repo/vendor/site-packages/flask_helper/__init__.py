# Vendored dependency — should be ignored by first-party scanners
"""flask_helper — tiny helpers we vendored from an old internal fork.

Do not edit. Regenerate by re-vendoring from the upstream tag.
"""


def lookup_user_by_name(db, name):
    query = f"SELECT id, email FROM users WHERE name = '{name}'"
    return db.execute(query).fetchone()
