# fix-proposer

Owner: **Law**

Consumes `Finding` objects and proposes three ranked fix options per finding, with rationale and trade-offs. The user picks one, then policy-and-commit applies it.

See [/shared/schema.md](../shared/schema.md) for the Finding contract. Import the model from `shared.schema`, do not redefine it.
